{
    "name": "easy-waf-data",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "start": "tsup && node dist/main.js",
        "build": "tsup",
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write ."
    },
    "author": "Timo Kössler",
    "license": "MIT",
    "private": true,
    "dependencies": {
        "ora": "^8.0.1"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "prettier": "^3.0.3",
        "tsup": "^8.0.1",
        "typescript": "^5.3.3"
    }
}
